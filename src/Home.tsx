function Home() {
  return (
    <div>
      <h1>Welcome to the React State Update Demo</h1>
      <p>This demo showcases how React handles state updates with different data structures.</p>
      <p>Navigate using the sidebar to view examples with objects and arrays, and see how React re-renders components based on state changes.</p>
    </div>
  );
}

export default Home;
